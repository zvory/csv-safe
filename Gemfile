source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in csv-safe.gemspec
gemspec
group :development, :test do
  gem 'pry', '~> 0.10.4'
  gem 'rubocop', '~> 0.58.2'
end

gem 'simplecov', require: false, group: :test

